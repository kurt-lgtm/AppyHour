---
phase: 02-shopify-sync-battle-test
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pipeline/pipeline_state.py
  - tests/test_pipeline_state.py
autonomous: true
requirements:
  - SYNC-03
  - SYNC-05

must_haves:
  truths:
    - "PassProgress serializes and deserializes commit_pending and errors fields without data loss"
    - "_active_prefixes(1) returns only PR-CJAM; _active_prefixes(2) returns CH/MT/AC/PK/TR prefixes"
    - "Existing checkpoints without new fields deserialize to empty defaults (no migration needed)"
  artifacts:
    - path: "pipeline/pipeline_state.py"
      provides: "PassProgress with commit_pending + errors fields; _active_prefixes() helper"
      exports: ["PassProgress", "_active_prefixes"]
    - path: "tests/test_pipeline_state.py"
      provides: "Tests for new fields serialization and prefix helper"
  key_links:
    - from: "pipeline/pipeline_state.py PassProgress"
      to: "tests/test_pipeline_state.py"
      via: "pytest import"
      pattern: "from pipeline.pipeline_state import PassProgress"
---

<objective>
Extend PassProgress dataclass with two new fields (commit_pending, errors) and add the _active_prefixes() helper that controls which SKU prefixes are active per pass number.

Purpose: These are the foundational data structures that Plans 02-02 and 02-03 depend on. commit_pending enables the error-but-applied detection (SYNC-05). errors stores per-order error messages for retry-failed (SYNC-03). _active_prefixes() is the single source of truth for pass filtering (D-06, D-08).

Output: Updated pipeline/pipeline_state.py and a test file covering new behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shopify-sync-battle-test/02-CONTEXT.md
@.planning/phases/02-shopify-sync-battle-test/02-RESEARCH.md
</context>

<interfaces>
<!-- Current PassProgress structure (pipeline/pipeline_state.py lines 26-49) -->
```python
@dataclass
class PassProgress:
    succeeded: list[str] = field(default_factory=list)
    failed: list[str] = field(default_factory=list)
    skipped: list[str] = field(default_factory=list)

    def to_dict(self) -> dict: ...
    @classmethod
    def from_dict(cls, d: dict) -> PassProgress: ...
```

<!-- Current PipelineStage enum values -->
```python
class PipelineStage(Enum):
    IDLE = 0
    INVENTORY_SYNCED = 1
    PASS1_COMPLETE = 2
    PASS2_COMPLETE = 3
    MATRIX_GENERATED = 4
    GIFTS_MERGED = 5
    FINALIZED = 6
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extend PassProgress with commit_pending and errors fields</name>
  <files>pipeline/pipeline_state.py, tests/test_pipeline_state.py</files>
  <behavior>
    - PassProgress() with no args has commit_pending=[] and errors={}
    - to_dict() includes "commit_pending" list and "errors" dict keys
    - from_dict() with missing keys defaults to [] and {} (backward compat with old checkpoints)
    - from_dict(to_dict(p)) round-trips correctly with non-empty commit_pending and errors
    - Existing succeeded/failed/skipped fields are unaffected
  </behavior>
  <action>
    RED: Write tests in tests/test_pipeline_state.py covering the behavior above. Run pytest — tests must fail (fields don't exist yet).

    GREEN: Add two fields to PassProgress dataclass in pipeline/pipeline_state.py:
      commit_pending: list[str] = field(default_factory=list)
      errors: dict[str, str] = field(default_factory=dict)

    Update to_dict() to include:
      "commit_pending": list(self.commit_pending),
      "errors": dict(self.errors),

    Update from_dict() to include:
      commit_pending=list(d.get("commit_pending", [])),
      errors=dict(d.get("errors", {})),

    IMMUTABILITY NOTE (per coding-style.md): Never append to commit_pending or errors directly.
    Callers must construct new PassProgress instances using dataclasses.replace() or spread syntax.
    The dataclass itself does not enforce this — it is the caller's responsibility.

    Run pytest — tests must pass.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_state.py -x -q</automated>
  </verify>
  <done>PassProgress has commit_pending and errors fields. to_dict/from_dict round-trips correctly. Old checkpoints without these fields deserialize to empty defaults.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add _active_prefixes() helper (per D-06, D-08)</name>
  <files>pipeline/pipeline_state.py, tests/test_pipeline_state.py</files>
  <behavior>
    - _active_prefixes(1) returns ("PR-CJAM",) — exactly this tuple, no other elements
    - _active_prefixes(2) returns ("CH-", "MT-", "AC-", "PK-", "TR-") — exactly these five
    - _active_prefixes(1) does NOT contain any of CH-/MT-/AC-/PK-/TR-
    - _active_prefixes(2) does NOT contain PR-CJAM
    - Function is importable from pipeline.pipeline_state
  </behavior>
  <action>
    RED: Add tests to tests/test_pipeline_state.py for the behavior above. Run pytest — new tests must fail.

    GREEN: Add to pipeline/pipeline_state.py (after PassProgress, before PipelineState):

    def _active_prefixes(pass_number: int) -> tuple[str, ...]:
        """Return the SKU prefixes active for a given pass number.

        Pass 1: PR-CJAM only (cheese jam pairings go on orders first).
        Pass 2: Standard food/packaging prefixes — PR-CJAM already present.

        Used by cmd_sync to filter which SKUs are processed per pass (D-06, D-08).
        """
        if pass_number == 1:
            return ("PR-CJAM",)
        return ("CH-", "MT-", "AC-", "PK-", "TR-")

    Run pytest — all tests must pass.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_state.py -x -q</automated>
  </verify>
  <done>_active_prefixes() exported from pipeline.pipeline_state. Returns correct prefix tuples for pass 1 and pass 2. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint file → PassProgress | Deserialized from JSON on disk; operator or crash could leave partial data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | PassProgress.from_dict | mitigate | Use .get() with safe defaults for all new fields; malformed checkpoint yields empty lists/dicts, not exception |
| T-02-02 | Denial of Service | errors dict unbounded growth | accept | Single pipeline run; dict bounded by order count (~2500 max); no persistent growth risk |
</threat_model>

<verification>
pytest tests/test_pipeline_state.py -x -q — all tests green
ruff check pipeline/pipeline_state.py tests/test_pipeline_state.py
</verification>

<success_criteria>
- PassProgress dataclass has commit_pending: list[str] and errors: dict[str, str] fields
- to_dict/from_dict handle new fields with backward-compatible defaults
- _active_prefixes(1) == ("PR-CJAM",) and _active_prefixes(2) == ("CH-", "MT-", "AC-", "PK-", "TR-")
- pytest tests/test_pipeline_state.py passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-shopify-sync-battle-test/02-01-SUMMARY.md`
</output>
