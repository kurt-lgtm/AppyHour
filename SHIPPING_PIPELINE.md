# 📦 AppyHour Shipping Pipeline — System of Record

> **This is the single source of truth for how AppyHour ships cold-chain boxes.**
> **GOLDEN RULE: if you change ANYTHING about the shipping process — a script, a rule, a carrier, a tag, a database, a threshold — you update THIS file in the same breath.** Out-of-date here = someone ships a warm box or burns money. Keep it current, keep it readable.

**Who this is for:** anyone — ops, CS, a new hire, an AI agent. No deep technical knowledge required to read §1–§3. Engineers go to §4+.
**Last updated:** 2026-07-21 · **Maintainer:** Kurt (Head of Ops)

---

## 📋 CHANGE LOG (newest first — add a row every time)
| Date | What changed | Why | Who |
|---|---|---|---|
| 2026-07-29b | **Veho SUSPENDED at ALL hubs indefinitely** (`24b8771`; Nashville was already off 7/24) + **`RELATIVE_DELAY_CEILING=1`** (`ee6b6a9`: push-lane delay gate = max(3%, FedEx's own ground 90d rate on the same hub→zip) — the flat 3% bought a same-lane-WORSE FedEx at 2.3×; +14 boxes/wk ≈ $7.2k/yr AND ~1.5 fewer expected lates/wk) + **`lib/flags.py` ONE flag-defaults table** (`a474b12`; Kori's hand-copied list had already drifted — build/Kori/qc/droplet all call `ensure_flag_defaults()`). Replay RMFG_20260724 with Veho dead: 288 boxes re-home (FedEx-Indy 149/OnTrac-Nash 62/UPS-Dallas 39), 0 leaks, **Indy still ~6 pallets on FedEx HD alone (crossdock stays)**, Veho-tax +$7.09/box ≈ $2.0k/wk; Midwest FedEx-failure reships now 2Day air (no 1-day ground left). | Veho service withdrawn (business); flat ceiling measurably over-bought FedEx; per-surface flag copies = engine divergence class. | Kurt + Claude |
| 2026-07-29 | **`HIST_SVC_PROMOTE=1` — history-proven lanes satisfy the serviceability gate.** A coverage-missing (carrier, hub, **zip3**) lane our OWN deliveries prove — n≥30 ground, ≥85% ≤2 lifetime, `lane_recency_bad` clean — now KEEPS its OnTrac/Veho tag instead of force-flipping to FedEx (`features.hist_serviceable`, shared by `engine.serviceability_gate` + `qc_audit.py`; recs flagged `_hist_svc`). 🔴 The OnTrac coverage sheet is confirmed LIVE yet omits lanes the network delivers (as LaserShip) — the sheet is the carrier's PROPOSAL; our history verifies both directions (the ≤2 gate already vetoes their over-claimed TNTs). Never STATE-level (the 6/29 hole stays closed); no recency bucket = no promote. Empirical A/B (wk0706+ RMFG tag-mismatch set): 42 promote-qualifying disobeyed boxes → 0 late/0 reship vs 617 non-qualifying → 20.9% late/22 reships. Recovers ~45 boxes/wk ≈ $28k/yr of FedEx over-buy. Kill `=0`. ShipRouting `8f2317b`/`f3d25ed`/`acc4ae3`/`061be9a`; ROUTING_RULES §0 + HISTORY_SERVICEABILITY same-day. | The 6/29 emergency fix (coverage = sole authority) was calibrated during an OnTrac delay-event week (24.8% late vs 1-3% neighbors) and silently dead-lettered the proven zip3 layer — buying FedEx at +$11.86/box on lanes OnTrac had demonstrably earned. | Kurt + Claude |
| 2026-07-24 | **Large-Box TNT re-home · MILP sees the carrier-TNT rescue · ShipStation spill-alt gate · Veho-TN SUSPENDED · decision log · quote-scope rule.** (1) **`LARGE_BOX_CLOSEST_HUB=1`** (build.py default ON; kill `=0`): a can't-air Large Box grounds at the hub with the **soonest ShipStation-committed ground TNT** (crow-distance only as no-key fallback), FedEx-pinned `!ANY FedEx - <hub>`, ice sized to the real quoted eff — was blind `!ANY - Dallas` (NY Large Box shipped Dallas ground ≈5d = warm). (2) **MILP reconciles the carrier-TNT rescue** — `build_feasibility` defers the §0 Dallas conversion and `_indy_milp_gate` calls the previously-orphaned `apply_carrier_tnt_rescue`, so a rescue onto Indy joins the contested set (captive → protected in Indy; 7 MA/WI Large Boxes were being "spilled" to a phantom Dallas eff2). (3) **FedEx/UPS spill alts ShipStation-gated**: Large = lazy positive ≤2 confirm per candidate alt; small/med = disk-cache contradiction only. **🔴 QUOTE SCOPE: ShipStation calls ONLY for lanes needing confirmation — never blanket batches** (v1 pre-quoted ~759 lanes → build network-bound 25min). (4) **Veho NASHVILLE SUSPENDED indefinitely** — `CARRIER_HUBS` Veho={Indianapolis}, config-overlay authority (`carrier_lane_overrides` in Kori settings); `nash_alt`/`reship_route` legal_lane bypasses fixed; duplicate legality dicts collapsed. (5) **`lib/decision_log.py`** — append-only per-order decision log (`_outputs/logs/routing_decisions.jsonl`), self-flags `large_box_far_dallas`. ShipRouting `8fe71ce` + quote-scope follow-up. ROUTING_RULES §0 same-day. | A NY Large Box cooked on a 5-day Dallas ground lane the engine chose by inheritance, the MILP was silently undoing correct rescues, and Veho went dark in TN — plus the build went network-bound from unscoped quote batches. | Kurt + Claude |
| 2026-07-21 | **Tuesday-run hardening: ship_date/ship_week split · ProductionDay TUE · inventory `available` calc fix · all-sub-cohort allocate guard · SUMMER_MAX_ICE.** (1) **cohort.json now carries THREE fields** — `tag` (RMFG sub-cohort), **`ship_date` = the ACTUAL departure day** (Mon for the Fri/Sat cohort, **TUE for a Tuesday sub-cohort**, Wed→Wed; drives ice/weather/filename/ProductionDay), and **`ship_week` = the `_SHIP_<Mon>` tag physically on the orders** (the ENGINE cohort key + the allocate scope). REPLACES the old "ship_date is always Monday" rule (that was wrong — it shifted a Tuesday batch's ice/filename to Monday). `lib/cohort` defaults ship_week from ship_date's Monday. ShipRouting `2e66e34`. (2) **ProductionDay derives from the ship weekday** (TUE/WED/else SAT) — was hardcoded SAT, mislabeled Tuesday cohorts. ShipRouting `a38c76e`. (3) **Inventory `available` push fixed** (`matrix_commander.cmd_allocate`): was `on_hand = HAVE − paid`, but Shopify `committed` spans ALL ship weeks → a week-scoped HAVE went to **available = −99 (CH-QOTA)** while the week was covered. Now writes **`available` = max(0, HAVE − week demand paid+$0)** directly. AppyHour `0559ec3`, MATRIX_RULES rule 12. (4) **All-sub-cohort allocate guard**: available must subtract EVERY sub-cohort's demand — all sub-cohorts share the one `_SHIP_<Mon>` tag, so allocate MUST scope on it, NOT a single RMFG sub-tag (which under-counts + over-promises). `compute_allocation` WARNs loudly on a non-`_SHIP_` tag. AppyHour `01990f5`. (5) **SUMMER_MAX_ICE=1** (set 7/18): forecast-independent 3×48 for all non-tray ground until Kurt flips it (summer over-ice bias is intentional). ShipRouting `3a612e1`/`f631f9e`. | A Tuesday sub-cohort exposed four latent bugs at once: the "always Monday" ship_date, the SAT ProductionDay, the on_hand-derived available (over-promise), and single-sub-cohort allocate scope. Inventory truth + cohort dates must be correct per departure day AND across all sub-cohorts of the week. | Kurt + Claude |
| 2026-07-19 | **Tray-only orders route (no AHB box SKU).** A tray-only order (#163677, 10 trays, no AHB line) was dropped by `box_simulation`/build → apply QC "untagged" hard-fail. Trays legitimately have no box SKU (Kurt); the pipeline now routes them (tray class → a FedEx hub dodging the Indy cap). ShipRouting `cf149eb`. | Tray-only cohorts silently vanished from routing → a hard QC fail near deadline. | Kurt + Claude |
| 2026-07-18 | **SUMMER_MAX_ICE=1 · coverage fail-closed · deploy-branch canon move · wk0720 matrix fixes · shipping.db path sweep.** (1) **SUMMER_MAX_ICE=1** (`build.py`): forecast-INDEPENDENT 3×48 for ALL non-tray ground until Kurt flips it — summer over-ice bias is intentional; `gen_rmfg_sheet`'s own 3×48 counter is UNRELIABLE (read 0 vs the canonical `ice_distvol_workflow`'s 552) → the ice workflow is the ONLY 3×48 source of truth. ShipRouting `3a612e1`/`f631f9e`. (2) **Coverage load fails CLOSED** — an empty/malformed OnTrac/Veho coverage file now REFUSES the build instead of silently routing with no serviceability authority. ShipRouting `53ed94f`. (3) **Deploy canon moved to `today-experiments-2026-07-03`** (supersedes `restore/cohort-scripts-2026-06`; wk0720 shipped from it). ShipRouting `9861a09`. (4) **wk0720 matrix fixes** — walnut header comma-pair (splits AC-FCWALN into two columns if not normalized), **MR-** (Cheese Journal) now pickable in pass2 active prefixes (was silently dropped every week), **Notes col ships EMPTY** (MATRIX_RULES 18). AppyHour `734debd`/`105aa98`. (5) **Canonical shipping.db = `C:\AppyHourData\shipping.db`** (env > canonical > legacy path sweep; never write the legacy copy). AppyHour `c40a649`. | Summer: ice-independent max is safer than trusting a forecast; a blank coverage file must fail loud, not route blind; wk0720 exposed walnut-split, dropped journals, and a stale DB path. | Kurt + Claude |
| 2026-07-17 | **P7 Phase A — server/ flow-api scaffold; ice STRUCTURALLY un-skippable; 3×48 SSOT + PO-box hard-fail.** (1) **`server/`** flow-api (FastAPI job-runner, confirm-token apply gate, ingest-worker skeleton, PG schema, Docker/systemd) — the strangler target so the weekly flow runs server-side (local stays authoritative until cutover). (2) **Ice is now un-skippable before the vF** (`IcePassResult` gate, ROUTING_RULES §13 same commit) — you cannot export a sheet that skipped the ice pass. (3) **3×48 count from the SSOT calc** (not `gen_rmfg_sheet`'s counter) + **PO-box address = hard FAIL, pull the order** (cold box can't ship to a PO box; UPS won't deliver). ShipRouting `660696f`/`1b13792`/`f3fa105`/`3188caf`. | Running build/apply by hand skipped ice + the vF; a PO-box or un-iced sheet reaching RMFG = a warm/undeliverable box. | Kurt + Claude |
| 2026-07-10 | **MATRIX_RULES 15-17 — col-L identity tags · reship 3×48 · one override per cohort.** (15) Col L (Tags) carries route + gel + `Gift_Redemption` + `Reship - <reason>` and NOTHING else/less (filtering to route+gel stripped gift/reship identity off the sheet); 15b: normalize the walnut header (strip comma) on BOTH matrix + gift sheets BEFORE union so AC-FCWALN stays ONE column. (16) Reships get 3×48 where slack allows REGARDLESS of forecast margin (an Arrived-Warm reship re-iced at the failed level re-runs the failure). (17) Ice `--write` is a mandatory flow step. AppyHour `4b4396e`, ShipRouting `8bbedec`. | A reship re-iced to the level that already failed ships warm again; filtering col L to route+gel erased gift/reship identity RMFG needs. | Kurt + Claude |
| 2026-07-05→08 | **Kori-editable ice toggles · 3×48 QC fix · P7 DigitalOcean design.** (1) `max_ice_nontray` + `export_3x48_upgrade` are Kori-editable settings (`lib/settings`); a 3×48 upgrade (2× ExtraGel48oz, no 24) now satisfies the max-ice check (1467 > 1222 BTU) — it was false-failing all 344 upgraded export rows. ShipRouting `bf1b1ff`/`4a06d02`. (2) **P7 = one DigitalOcean droplet + managed Postgres**, single-writer ingest, strangler migration off local shipping.db (existing Cut-Order droplet @142.93.188.10 reused, resize to 4GB). ShipRouting `4ec5f8b`/`48b6a7c`/`6616bb9`. | The 3×48 upgrade was being rejected by its own QC; P7 moves the flow off the single local machine (the only-copy risk). | Kurt + Claude |
| 2026-07-03 | **P6 — ONE SEAMLESS FLOW (`weekly_flow.py`): SKU checks → routing+ice apply → vF export.** (1) `weekly_flow.py` runs the whole week in one command — matrix generate + product checks + inventory allocate (`--allocate` commits) → `build.py`+`apply.py` (routing+ice, `--apply-tags`) → `gen_rmfg_sheet` self-QC → vF. **Running build/apply by hand skips stage 1 (matrix+gift) and stage 3 (vF+self-QC) — use the flow.** (2) **`cohort.json` + `lib/cohort` = cohort of record** (no stale hardcoded `_SHIP_` defaults; SHIP_TAG env removed the `_SHIP_2026-06-29` no-op bug class). (3) **Canonical 5-form tag grammar in `lib/canon`** — the ONLY tag parser/emitter (parse/validate_combo); never hand-roll a tag regex. (4) **`lib/qc_gate` = ONE routing+ice QC gate** wired into `apply.py` pre-write AND `gen_rmfg_sheet` export (one gate, two chokepoints). (5) **Engine OWNS ice** — gel config written alongside routing in one pass. (6) **FedEx-HD → `!ANY FedEx - <hub>` positive pin** for all hubs (was a leaky `!NO` single-hub fence; RMFG picks Ground-vs-Home by address). (7) **Fixed_Route-aware QC + fence**; `wx_margin_cutoff` Kori-editable. ShipRouting `8b98970`/`7a10dd3`/`2322c67`/`7878089`/`ec00884`/`bceb7b5`/`345b4f5`. | The week was a pile of hand-run scripts that skipped ice/vF/QC; one gated flow + one cohort-of-record + one tag parser + one QC gate kills the drift/no-op/leaky-fence classes. | Kurt + Claude |
| 2026-07-02 | **MILP CANON + HD fence + RESHIP RECOVERY + shared serviceability gate + watchdog pair; Kori full parity.** (1) **MILP_LIVE=1 CANON** (`build.py` default ON): the HiGHS solver now MAKES the live Indy keep/spill decision (`engine._indy_milp_gate`); greedy runs as shadow; ice re-sizes off the spilled lane's `_ice_eff`; snapshots feed `milp/postmortem_ab.py` (now with warm-rate + invoiced-$ joins). Kill `MILP_LIVE=0`. (2) **FENCE_FEDEX_HD=1**: sole positive `!FedEx Home Delivery` pins become FedEx-only fences (RMFG picks Grd-vs-HD by address res/comm); Indy excluded. (3) **RESHIP_RECOVERY=1**: carrier-failure reships (both `Reship - <reason>` and nested `Shipping::` tags) → proven healthy 1-day ground excluding the FAILED carrier (derived read-only from fulfillments) else 2Day AIR; the carrier-TNT rescue can no longer re-ground an air'd reship. Root causes on 6/29: rescue re-grounding + the `_RESHIP_FAILED_` deriver lost in a refactor. (4) **Serviceability gate moved to `lib/engine.serviceability_gate`** so Kori runs the identical pass (it skipped it before); Kori also mirrors all flags, threads spill/svc/fence apply-overrides, and gained an explicit Cohort SAT/TUE selector. (5) **Watchdog pair**: `scripts/automation_health.py` daily (dead-man-switch — absent heartbeats, failed schtasks, stale ingest, db quick_check; HEARTBEAT_RULES.md) + `ShipRouting/scripts/loop_scorecard.py` weekly (reship-recovery check, Indy pins, MILP A/B). (6) **QC**: pandera schema gate in `qc_audit.py`, log-only (QC_SCHEMAS_RULES.md); matrix REST tag-filter bug fixed in 3 files (param silently ignored → whole-store fetches; MATRIX_RULES.md rule 3). (7) **Single-writer lock Phase 1** (`appyhour_lib/db.py` writelock) + bash-gate/settings enforcement of the shipping.db read-only rule. | One day, one theme: make the decision engine canonical (MILP), make every caller run the same guards (Kori parity), make failures loud (watchdogs), and make the rules machine-checked (schema gate). | Kurt + Claude |
| 2026-06-25→27 | **Live weather canon (daily-high basis) + STATE-layer drop + zip-serviceability gate + 8-day/Friday-zip5 forecast.** USE_LIVE_WEATHER=1 (OWM cohort forecast cache, `window_daily_high` — the mean-temp basis ran 10-15°F cool); HISTORY_SERVICEABILITY STATE layer DROPPED after 358-391 unserviced-zip mis-routes on _SHIP_2026-06-29 (metro deliveries falsely proved rural zips); post-routing ZIP-SERVICEABILITY gate hard-reroutes uncovered Veho/OnTrac → FedEx HD + asserts; `load_veho` now pulls the LIVE public serviceable-zips API; OWM One Call 8-day daily reaches the Wed delivery day, Friday runs auto-switch to zip5 (`friday_forecast_refresh.py` + scheduled task). shipping.db corruption 6/27 → mandatory `appyhour_lib/db.py` connect helper + integrity-gated post-ingest backup. | Coverage files are the sole serviceability authority; history refines TNT only. Live weather routes reality, not season-mixed floors. | Kurt + Claude |
| 2026-06-24 | **CARRIER-TNT TRUST + CLOSEST-HUB live; carrier-hub legality guard; Veho@Dallas data fix; carrier sync fix; MILP Indy epic (now the Indy decision via human-reviewed draft).** (1) **CARRIER_TNT_TRUST=1 live** (`build.py`): air-bound orders are rescued onto a FedEx/UPS GROUND lane the carrier itself quotes ≤2 via owned **ShipStation/ShipEngine** (`lib/carrier_tnt.py`), +1 ice, guarded by `legal_lane` + demote-guard; A/B 6/29 air 72→12, 0 regressions. Post-ship audit layer **`carrier_tnt_audit.py`** grades quoted-vs-actual TNT, and **every build writes `carrier_tnt_rescues.json`** (Kori-snapshot style). Needs `%APPDATA%/AppyHour/shipengine_api_key.txt`; **no key → safe no-op**. Kill: `=0`. (2) **CLOSEST_HUB_DEFAULT=1 live** (P4 phase-1): prefer the geographically closest PROVEN hub (distance tiebreak; a farther hub wins only if clearly cheaper); fixes cost-ties that broke toward Indy by lane-build order (trims Indy demand ~6%). (3) **Carrier-hub legality guard** (`lib/features.legal_lane`/`CARRIER_HUBS`): rejects physically-impossible lanes (Veho=Nashville+Indy only/never Dallas, UPS=Dallas-only, OnTrac=Anaheim+Nashville+Dallas, FedEx=all 4) at `build_history_lanes` + `choose_lane` AND at invoice ingest — caught the engine proposing `Veho - Dallas` tags. (4) **475 mis-hubbed Veho@Dallas rows re-attributed** Indy/Nashville; root-caused to the Veho parser trusting the injection-market DC over Origin Zip — fixed `parsers/veho.py` + added an all-carrier ingest guard in `shipping_invoice_db.py` (dev+prod). (5) **`sync_all_carriers.py` now pulls FedEx+OnTrac+Veho** (was FedEx-only → OnTrac/Veho invoices silently lagged); ingested OnTrac/Veho through the 6/15 ship-week. (6) **MILP Indy-capacity epic** merged to canon (`ShipRouting/milp/`): a global HiGHS solve replaces the greedy 6-pallet Indy gate's keep/spill choice with the globally cheapest set. **PROMOTED from shadow to a DECISION step** — `milp/draft_sheet.py` runs the cohort through carrier-TNT + the MILP and emits a DRAFT routing xlsx for **HUMAN REVIEW before apply** (human-gated, not autonomous; live `compute_routing` still calls the greedy `_indy_pallet_gate` as the shipped path until the draft workflow is adopted). A/B: ~$627/wk hard carrier+ice CASH saved BUT +$234/wk expected warm-arrival RISK → **~$393/wk NET** (a cash-vs-spoilage trade; quote the NET). 6/29 draft: 1664 orders, 41 carrier-TNT rescues, Indy 5.997/6, MILP differs from live greedy on 126 orders. | Lane visibility + capacity were the session's themes: vouch lanes we don't ship (carrier-TNT), route to the closest cheap hub (closest-hub), stop impossible lanes from dirty data (legality), and optimize the one hard cap (Indy MILP, now a human-reviewed draft decision). | Kurt + Claude |
| 2026-06-23 | **ENGINE FIX — Monday/Saturday carrier history now GROUND-only (air/ground contamination killed for both cohorts).** `engine.py compute_routing` changed `build_carrier_hist(ground_only=dallas_only)` → `ground_only=True`. The `(carrier,hub)` bucket qualifies GROUND lanes only (air floor is fixed cost, no history), so merging FedEx **2Day air** with **Home Delivery ground** had diluted slow ground to look 2-day: **Dallas-FedEx ground read 85% ≤2 but its TRUE ground rate is 56%**; **23 NC/FL/CA (hub,zip3) lanes mis-passed as proven-2-day-ground when real FedEx ground there hits 2-day 0% of the time.** Now fenced → those orders route OnTrac/Veho/UPS ground or 2Day air. Validated read-only first (`validate_ground_only_history.py`). Tuesday already had this (interim); now Monday too. Tradeoff: ground-only uses shipments-only (drops the service-blank delivery_status backfill — which was contaminating anyway). | The exact "Dallas-FedEx-ground = failure lane" delays were caused by the engine trusting air-diluted ground history; ground-only history fixes it at the source. | Kurt + Claude |
| 2026-06-23 | **Canon hub/tag refinements + injection-vs-transit resolved.** Hubs are last-mile PICKUP STATES — **5: TX/Dallas, TN/Nashville, CA/Anaheim, IN/Indianapolis, MA/Woburn(DEPRECATED, no live rows)**; `normalize_hub` now accepts state codes. Added **`canon.parse_routing_tag()`/`assigned_hub()`** — THE correct tag reader (skips `!NO …` blocks); replaces hand-rolled `'Dallas_AHB!' in tags`, which mis-read Veho/OnTrac as Dallas (the bug bit twice). **Injection hub (routing tag) vs transit hub (PP `origin_hub`) AGREE 97%** (OnTrac 99/FedEx 95/Veho 98/UPS 98); the 3% disagreement (88% → Dallas) is **NOT benign** — a TNT discriminator proved ~150 are **mis-fulfillments** (140 FL routed Nashville, physically shipped Dallas, delivered 3.24d > both Nashville/Dallas baselines = TNT3 late). **assigned (tag) ≠ actual (PP/invoice) is a mis-fulfillment FLAG, esp. with TNT degradation — store BOTH, never silently trust the tag.** Off-schedule pickups (Wed–Sun, 8,071) are **Veho's normal Fri/Sat regional cadence** (5,699) — not internal (0%), not reships (1%). Docs: vault `Canonical Dimension Layer.md` + `ShipRouting/CANON.md`. | One correct dimension reader kills the parser-bug class; resolving injection/transit removes the "hubs are ambiguous" false alarm (it was my parse bug). | Kurt + Claude |
| 2026-06-23 | **Canonical dimension layer + DB hygiene audit (swarm).** Established the canonical unit: a **LANE = (service_level, hub, carrier)**. New `ShipRouting/lib/canon.py` is the single source of truth — `normalize_service`→{Ground, Home Delivery, 2Day, Overnight, Unknown}, `normalize_hub` (strips `AHB`, HQ_IGNORE/Unknown/''→NULL), `normalize_carrier` (LaserShip→OnTrac), `is_ground` (Ground+Home Delivery; the predicate that keeps AIR out of ground buckets). **Root cause fixed:** `build_carrier_hist` merged FedEx **2Day air** with **Home Delivery ground** (and UPS 2nd-Day-Air) into one `(carrier,hub)` bucket → fast air diluted the ground late-rate → slow ground lanes mis-passed as 2-day (CO 80906, far-FL, HI). Fix = key history by the full lane triple; air keeps its own slice. New read-only gate `ShipRouting/db_hygiene.py` (23 invariants, severity-sorted) — baseline 16 violations: HUB-002 blank hub 7,896 (CRITICAL), SERVICE-001 un-normalized 50k, TRANSIT-002 null 16k, HQ_IGNORE 4.3k, orphans 1k, feedback 5-digit 1.4k. NOTE: **no true (invoice_id,tracking) dups** — the "17%" was NULL invoice_ids. Remediation scripts authored but **NOT applied** (adversarial critic flagged data-loss risk + unvalidated); canon layer + detection gate ARE live. Full audit: `ShipRouting/DB_HYGIENE_PLAN_2026-06-23.md`. | Un-normalized dimensions + mixed-service history silently corrupted routing qualification; one canonical layer at the source kills the whole contamination class. | Kurt + Claude |
| 2026-06-19 | **Routing + delivery-status data-integrity hardening.** ShipRouting: **R1 reships now recover onto proven 1-day ground across all four carriers** (cheapest-first Veho < OnTrac < UPS < FedEx; UPS/FedEx require real ground history `carrier_eff==1`; Express only when no carrier proves 1-day); genuine carrier/thermal failures only (Delayed/Lost/Misdelivered/Warm/Damaged/Ice Pack), while mispick/courtesy reasons route normal ground. **R2 reships also avoid currently-degrading 1-day lanes** using the same shared `lane_recency_bad(bkt, zip5, state, temp)` predicate as `choose_lane` (K=3 recent streak / tiered >10% late-rate; cold exempt). **R3 Fixed_Route locks are protected inside the Indy pallet gate**: they are excluded from eviction like Indy-captives; only a loud last-resort manual breach can move one. Earlier same effort: G1 delete-dead-trial, G2 ice-gate trusts router verdict, A2 keep-optimistic-cool-map. Data pipeline: **delivery_status freeze fixed** — 52,719 rows stuck as `lost_in_transit` from Mar-Apr aged out of the sync window; `reconcile_lost_in_transit.py` recovered 28,417 from carrier invoices with real delivery dates, and `daily_shipping_sync.aged_out_sweep()` retires the rest to new terminal `status='aged_out'` every daily sync; re-pull window 30→45d; Wednesday ops now includes a delivery_status hygiene check. Net: lost_in_transit 52,719→0; in_transit 1,438→85 real. Routing engine was never affected because frozen rows had no delivery_date and were already excluded from `build_hist`. | Reships should use cheap proven 1-day ground without reusing lanes currently failing; customer locks must survive capacity pressure; stale status rows must not masquerade as live in-transit packages. | Claude + Codex |
| 2026-06-19 | **6/22 cohort ROUTED + ICED + APPLIED to Shopify (2341 orders, 0 fail) + 6 engine fixes.** (1) **Ice-shortfall TOLERANCE band (200 BTU)** — a ≤2-day lane that's max-ice-exceeded by ≤200 BTU (≈0.8 of a 24oz pack ≈ +5.6°F → ~55°F arrival) on a feasible non-Indy hub now GROUNDS instead of Expressing; >200 BTU / floor>2 still Express (`optimizer.ICE_TOLERANCE_BTU`, `min_ice` exposes `shortfall_btu`; `invariants` G5 → WARN when tolerated). (2) **LIVE hub origin temps** — `_HUB_TEMP` static dict (Nashville 85 etc.) replaced by live OWM window-avg per hub zip (killed the static-85 over-Express; MA-from-TN went from "needs air" to feasible). (3) **Feasible-hub fence (EXPRESS must be PROVEN)** — data-sparse orders fence-ground at a FedEx-published-TNT≤2 hub instead of flying; temp-gated (HOT≥85°F needs measured proof, COOL grounds optimistically). (4) **HOT gate fixed 50°F→85°F** (`PROBATION_MAX_TEMP`) — the 50°F gel threshold made every summer dest "hot" and killed optimistic ground → 89 false Express; at 85°F → 48–52 (proven-necessary only). (5) **Indy fence hardened** — fence-out now blocks BOTH Veho+FedEx-Indy (was FedEx-only; `ve`-gated so no phantom block; OnTrac has no Indy lane). (6) **`box_simulation` lineItems `first:100`→`first:50`** — Shopify silently dropped multi-line **bundle orders** (cost-throttle) from the cohort fetch → 8 orders (incl #153671) never routed/iced; now all returned. Plus: **`apply_reship_max_ice_v2.py`** (parameterized, was 6/15-hardcoded — 15/18 reships were un-iced); **`apply.py` now gates writes through Kori's canonical `is_approved_bang_tag`** (caught a live `!!FedEx…` malformed tag). Liberal max-ice applied cohort-wide (Kurt "just this once"). **6/22 result: 2341 routed, 52 Express (~2.2%), Indy 6 pallets/breach=0, 0 tag conflicts.** Recharge forecast excluded from tagging. Codex-built core + Claude-verified. Commits ShipRouting `efd15be/e85b65f/0930999/235f619`+fences, AppyHour box_simulation. | Static hub temp + a 50°F "hot" line over-Expressed all summer; bundle orders silently missing; reships un-iced; a malformed `!!` tag slipped live. North star: every box ≤2-day, cold, lowest cost — proven, not assumed. | Kurt + Claude + Codex |
| 2026-06-18 | **`feedback.date_reported` normalized to ISO YYYY-MM-DD (format-mess fix + carrier-resolution fallback).** The ticket table had 5 date formats (dominant: year-less `Month-D` from ad-hoc backfills; plus `MM/DD/YYYY`, `M/D/YYYY`, `Dec 6`). Year-less dates are un-sortable, and a normalizer guessing the current year made **2025 tickets masquerade as 2026** → polluted the current-week issue report. `shipping_report_by_date.sql` was returning `_no rows_` (lexical `BETWEEN 'June-8' AND 'June-14'` is empty since `'8'>'1'`); the report also showed `unknown` carriers (5-digit non-Shopify order#s that don't join to `fulfillments`, carrier sitting unused in `feedback.carrier`). **Fixes:** (1) one-time backfill `scripts/incident-fixes/normalize_feedback_dates.py` rewrote **2742 rows → ISO** (year inferred: latest year keeping `date ≤ synced_at`), backup table `feedback_backup_20260618_134251`; (2) hardened the recurring DB chokepoint — `_tee_to_shipping_db` in `gorgias_sheets_sync.py` now `_normalize_date_reported()`s every insert to ISO (sheet col A stays MM/DD/YYYY for Ops formulas); (3) consumers updated to ISO: `shipping_report_by_date.sql` (ISO-passthrough + `feedback.carrier` fallback so no join-miss = `unknown` + 6-digit Shopify floor `>=100000`), `weekly_carrier_report.py` (Month-D→ISO token convert), `warm_cohort_report.py` (ISO parse). Last-ship-week (_SHIP_2026-06-08) issue count went 27→**45**, zero unknown, stale 2025 rows excluded. All 3 reports now agree. | Five date formats + year-less text broke windowing and let 2025 tickets count as this week; carrier was resolvable but unread. | Kurt + Claude |
| 2026-06-17 | **Thermal guards + proof-bar + reship rules (engine hardening, branch `fix/air-pollutes-ground-lane-proof`).** (1) **Season-aware thermal guards G-A/B/C** — `lib/zone_floor.py` zone ice-floor (`eff_for_ice = max(routing_eff, p75/p90 GROUND transit per hub→zip3)`) threaded through every ice path so a wrong-low routing eff can't under-ice; `lib/invariants.py` pre-apply HARD/WARN checks (zero-ground-history, eff≤2 w/ no samples, island+ground, floor>2 on ≤2 tag, ice-exceeded), heat-gated (cold dest = WARN, only HOT blocks); `apply.py INVARIANT_GATE` **ARMED** (HARD blocks `--apply` unless `--force-override`). Built by Codex, Claude verified + fixed a G5 false-positive storm (805→54 HARD) + `_no_air_remedy` (TX/already-Express → WARN). Kori inherits the floor (GelPack `77541b6`). (2) **Exact-zip5 proof bar = 3** (`Z5_MIN_N`, was 5) — ≥3 deliveries to the SAME zip proves a lane; zip3-pooled still needs 5. (3) **Push lane trusts measured median over a pessimistic carrier quote** (OnTrac quoted OR 97405 at 3d but runs 2d, n=7 → now grounds). (4) **Reason-aware reships: "Reship - Delayed in Transit" → FedEx 2Day AIR** (reliability; don't re-gamble ground) even if a 1-day lane exists; other reasons keep 1-day-ground-else-Express. 71 tests. NOT applied; 6/22 + Tuesday held. Investigations: `ShipRouting/INVESTIGATE.md` (INV-1 OnTrac quote/coverage, INV-2 FedEx/UPS live-TNT API for the data-sparse Express bucket). | Routing eff was the sole input to ice (a routing bug = a warm box); thin/quote-pessimistic data over-flew provable ground lanes; delayed reships need reliable air. | Kurt + Claude + Codex |
| 2026-06-17 | **Service-pollution bug fixed — air history was proving out GROUND lanes (new §3 rule #10).** `build_carrier_hist` keyed FedEx/UPS history by `(carrier, hub)` with NO service filter, so FedEx 2Day *air* (6,948 rows = 32% of FedEx volume, ~2-day because it flies) pooled into the FedEx Home Delivery *ground* bucket → `carrier_eff` returned 2 from air → `choose_lane` tagged ground on lanes that ground in 4-5 days (order 152472: Dallas→OR-coast "proven 2-day sole survivor"). The polluted bucket also fed `state_eff` (trial), `lane_late_rate` (delay gate), `best_rmfg_eff` (cross-hub). **Blast radius: 111 already-shipped Tuesday orders (RMFG_20260616)** on air-proven ground tags (58 no ground history at all incl. Hawaii-by-Dallas-ground; 8 at 4-5 real ground days) — iced for 2-day, on a 3-5 day truck. **Fix (scope A):** `lib/origin.is_ground_service()` whitelist (Home Delivery/Ground/Ground Plus/RD); `build_carrier_hist` + `build_rate_card` gated to ground services (Express floor uses its own EXPRESS_COST, untouched). Re-route: 6/15 130→10 air, 6/22 21→36 air (1.6%), no coverage collapse. 58 tests. Branch `fix/air-pollutes-ground-lane-proof` `19ea980`. **DEFERRED (sample study):** postmortem warm-rate (state-only, upgrade-only→safe), `build_warm_rates` (hub-blind). **HELD:** 6/22 apply + 111-order warm watchlist await Kurt. Vault: `decisions/air-pollutes-ground-lane-proof-2026-06-17.md`. | Air and ground are different products (~2-day air ≠ 4-5-day ground); pooling them under-iced cross-country boxes. Every lane decision must key carrier×hub×service. | Kurt + Claude |
| 2026-06-16 | **Express floor overhaul + force_2day override retirement.** Engine floor no longer Expresses on thin evidence: (1) LAST-PROVEN-LANE rescue — keep a proven <=2 lane that the 3% delay gate only marginally demoted, when it's the only <=2 option (recovers OnTrac/Indy 1-day lanes that were dumped to air); (2) "prove slow first" TRIALS — an unproven lane with proven STATE-eff <=2 is tried on ground (insurance ice +1d, +2d in heat) instead of Expressed, **capped at 90F** (above 90 too hot to gamble -> Express stands); (3) **TX always ships ground** (Dallas-region, never air — all paths incl reship/Indy-bump); (4) **Large Box never air-Express** (OneRate cost-prohibitive -> !ANY Dallas ground); (5) reason strings now specific ("No proven safe lane: <causes>" / TRIAL / LAST-PROVEN / TX floor) flowing into the sheet Rule Check; (6) additive `lane_proof` instrumentation -> cohort_sheet "Floor Rationale" tab. Cohort impact (6/22): ~136 orders/cohort off Express (Express ~halved). **Override retirement (Dan's challenge): all 23 live `force_2day` AIR overrides retired** — 22 dropped (engine routes proven ground <=2), MN 551 converted to a GROUND lane (FedEx-Home-Indy 2d); validated vs `shipments.transit_days` ground history (23/23 unsupported, ground 2-day everywhere). ~$50k/yr recovered. ShipRouting commits `6b6fcd4..1134760`; config backups in `_outputs/cache/`. 52 tests green. Vault: `decisions/force-2day-override-retirement-2026-06-16.md`. | Express was firing on stale/inflated evidence + 23 overrides forced air where ground is 2-day; ice handles heat, routing handles TNT. | Kurt + Claude + Codex |
| 2026-06-14 | **Codex audit → 3 ice-sizing fixes (under-ice closed).** Independent Codex audit of the 6/13 convergence found real bugs; each verified against source + adversarially re-verified (3 refuters + consumer trace). **Fix A (finding 2, BLOCKER):** `_indy_pallet_gate` rerouted a bumped Indy order's tag but left `best_val`/`corrected` at the stale Indy eff → `resolve_ice_target`'s `eff_days` sized a 1-day Indy order's gel for 1 day on its bumped 2-day lane = **under-iced** (violates "ice is a MAX"). Now `best_val = max(_indy_eff, alt_eff)` (or `MAX_TNT` for Express) + clear `corrected` → eff only ever rises; ICE-RECHECK now actually wired. Proven monotonic-up + 6-pallet cap-assert intact. **Fix B (finding 5):** `routing_v2` sized shadow ice on the engine's PROPOSED tag, but `apply_shadow_ice --apply` applies the gel tags regardless of which route wins → Fixed_Route/kept-existing overrides iced at the wrong origin hub. Ice now sized AFTER the apply hierarchy at `ice_tag` = the real ship hub (`tags.ship_origin_tag()` pulls the single positive-assign member past `!NO` fences; ambiguous → flat-dest). First pass naively used the raw comma-joined `final_tag` → adversarial refuter caught it **under-icing** a hot-hub(Dallas 92/Nashville 85)→cool-dest 1-day lane (flat-dest drops the hot-origin half); `ship_origin_tag` recovers the true hub. **Fix C (finding 4, MAJOR):** `veho_cap_pressure_engine` read the LEGACY side via `load_veho()` which respects the now-written cutover flag → re-runs compared cutover-vs-cutover = false 0 creep; now reads the `VEHO` constant directly (verified live: 7452 vs 7613 zips, creep real). Original ACCEPT decision stands (legacy 604≠new 696 proves flag was off then). Commits: ShipRouting `b709555/7ff8d74/6553268`, GelPack `62e032a`. 36/36 tests (was 29; +5 bump-eff + ship_origin_tag). | The convergence introduced a stale-eff under-ice on Indy-overflow orders + a wrong-origin seam on locked routes; Codex caught both, adversarial verify caught a regression in the first B patch. | Kurt + Claude + Codex |
| 2026-06-13 (pm) | **EXECUTE batch: Veho ingest scaffold + ice-path convergence finished.** (0) offsite backup of known-good state. (1) `routing_v2.py` hub-parse cleanup → `lib/tags.origin_hub_from_tag` (5 tests). (2) **G6.1** `load_veho` cutover-gated: legacy by default, reads new GroundPlusSuite coverage ONLY when `veho_cutover.flag` set (watcher stages `veho_ground_plus.cutover.xlsx`) — engine can't cut onto +764 Indy zips before the cap check. (3) **G6.2** `scripts/veho_cap_pressure.py` (observe-only) → **verdict CAP_ELIGIBILITY: 869 candidate Indy pins = 2.07× the 6-pallet cap; 127 Express/$3025 ≫ threshold → flag UNWRITTEN, engine stays on legacy coverage.** (4) **G18.3:** `seasonal_floor` now unconditional 50-state warm-month floor (≥80F every state) → **step-D summer guard RETIRED** (its job folded into sizing_temp, on the real box; equivalence test pins it) + **G17** per-order gel logging (interrupt-safe). (5b) **MULTI-FACTOR Indy eviction** (Kurt: "not just cost. tnt and risk too"): the pallet gate now evicts the LOWEST spill-cost order within a tier first (`spill_cost = dest_heat × (1+alt_slowdown)`) → keeps hot-dest/slower-alt orders on the fast Indy lane, evicts cool/same-speed first. Hard 6-pallet cap invariant unchanged. (5c) **`resolve_ice_target`** — one shared pure ice decision (sizing_temp + origin + calibrated_ice); batch wired to it (byte-identical), Kori-GUI adoption deferred to a focused session. (6) **G6.2 AUTHORITATIVE** (`scripts/veho_cap_pressure_engine.py`, `7be6f8b`): live `compute_routing` legacy-vs-new + the multi-factor gate's OWN stats. **Real Indy demand 604→696; gate evicts overflow to Nashville-2day (all tier0, no ice change) because the risk-aware ranking keeps would-Express orders on Indy → NET cap-forced Express creep = 0 → VERDICT ACCEPT.** Flat-ladder estimate (127 Express) was 2×-over pessimistic. **CUTOVER LIVE (Kurt "accept" 2026-06-13): flag written, verified — `load_veho` now serves the new coverage (7613 zips / 7419 active, `_veho_path`→CUTOVER). Next cohort routes on 6/9 GroundPlusSuite; multi-factor gate holds Indy ≤6 pallets, 0 added Express. REVERT: delete `%APPDATA%/AppyHour/routing/veho_cutover.flag`.** Commits: ShipRouting `3aaa483/6c5be90/3f65fd2/374ff28/7898480/0f46c96/1713347/7be6f8b`, GelPack `7b79b15/8812f5a/565702a`. 29/29 tests. | New coverage doubles Indy demand vs a hard 6-pallet cap → gate it before cutover; finish one ice path (no summer-guard bolt-on). | Kurt + Claude |
| 2026-06-13 | **ICE-PATH CONVERGENCE (G18.x) + apply-path fix (G20) + --tag (G19).** ONE ice sizing temp: `optimizer.sizing_temp()` = max(live forecast, warm-season floor month-gated, yearly `dest_temp`) — wraps `dest_temp` (no dup fn), merges the two old fallback dicts (`WEATHER_FALLBACK_F` + `_STATE_TEMP`). Both batch (`routing_v2`) + Kori-live ice now read it (avg basis, not peak, per Kurt). Engine ice now **splits origin/dest** transit (`min_ice(origin_temp=)`, `_HUB_TEMP`) like Kori `analyze_order`. **G20:** Kori "Apply" was rebuilding routing from legacy `routing_tags[state]` — now reads the engine decision (`suggested_routing`). **G19:** `--tag` CLI retires hardcoded cohort. Commits: ShipRouting `2ee6d4d`/`d5b33d5`/`fc583b2`/`5fa94aa`/`a1aec4d`, GelPack `1d5eff9`/`424b838`/`7422180`/`9d8dd00`. 21 tests green. NOTE: summer-guard step D still live (belt-and-suspenders) until G18.3 unified applier. Also: **Veho TNT pulled live** (DocSend, passcode→ACL'd file); seasonal-ceiling (G21) + Veho-ingest (G6) DESIGNED w/ Indy cap-pressure gate. | Three ice paths + two temp dicts could disagree per order; Kori showed engine routing but applied legacy. Convergence = one temp, one decision, never under-ices. | Kurt + Claude |
| 2026-06-12 | **Indy pallet gate fix + violation found.** The 6-pallet Indianapolis cap is a CROSSDOCK capacity — both Veho-IND and FedEx-Home-Delivery-IND inject there. The gate counted only Veho, so Home-Delivery was invisible and Indy blew the cap: 6/15 = 428 Veho "passed at 6 pallets" while +150 Home Delivery made the TRUE load 578 (~8-9 pallets, ~50% over). Gate now counts EVERY positive `Indianapolis_AHB!` assignment. ⚠️ 6/15 was built+applied PRE-fix → its live Indy load is over cap (re-gate decision pending). Also documented: anomaly scan, eff-TNT=real (G13), engine=config (G14), trial lanes, auto course-corrections — reconciled across SKILL + vault. | Crossdock can't stage >6 pallets; uncounted Home-Delivery risks boxes sitting/delayed (the exact thing the cap prevents). | Kurt + Claude |
| 2026-06-12 | **ADDRESS-QUALITY CLOSED LOOP defined (G12) + first manual run.** Two detectors → one fixer: pre-ship `invalid_address` tag (EasyPost; 63% rural false-positives) + post-ship invoice address-correction fees (~$24 ea, corrected address in FedEx detail CSV). Fixer: merge-split → typo-fix → maps-verify → clear false positives (bare unit numbers KEPT) → ambiguous to Kurt. Today: 38 live triaged, 12 orders fixed + 25 untagged (37/37 ok) → **6/15 cohort address-clean**. Customer-default + Recharge propagation BLOCKED on scopes (Shopify `write_customers`, Recharge address-write) — renewals re-break until granted. Rule: REMOVE tag on fix (was never removed; 100+ stale). Also: anomaly_scan.py shipped (7 detectors, severity-sorted) — findings fold into the Slack issues report + cost-sheet ANOMALIES tab as ADDITIONS. | EasyPost detects but can't fix; subscribers pay correction fees monthly until source address fixed. | Kurt + Claude |
| 2026-06-11 | **Unknown-triage + weather actuals + Express probation.** (a) 906 "unknowns" = 94% Downloads noise; real find: **UPS RMFG breakdown pattern was MISSING** — 5/25+6/1 invoices (acct 2H9494) unparsed → pattern added (GelPack@1d35d4e), RMFG PDFs/old FedEx naming exempt, counter now signal-only. (b) `weather_history` was DEAD since Apr-17 (never task-scheduled) → 60d backfill run + daily 3am task; engine `dest_temp` + postmortem warm-analysis read this. (c) EXPRESS-PROBATION tier live (ShipRouting@507c257): no-local-data FedEx/UPS lane qualifies off state median when 0 proven lanes, temp<85°F, ice+1, self-demoting — shadow 76 rescues ≈ $1.4k/cohort. Vault + SKILL rulebook synced. | Fix what's fixable now; Express relaxation per positive-tagging era. | Kurt + Claude |
| 2026-06-11 | **M1 CUT-OVER COMPLETE — ONE DATABASE.** Canonical = `%APPDATA%/AppyHour/shipping.db`, sole importer = `auto_import.py` (new code live-proven: 0 dup tracking, 22,881 lane keys intact, `shipment_dims` created +1,193 rows). Dead pipeline retired (`ShippingReports/_retired/`), dead DB archived (`output/shipments.db.RETIRED-2026-06-11`). Branches merged (nested `master`@0f6f577, parent `main`@0f2b2a6). B-INGEST-1 healed (re-imports now fix routing fields). EXPRESS-PROBATION tier live in engine (ShipRouting@507c257, shadow: 76 rescues ≈ $1.4k/cohort, temp-gated 85°F). Backlog → M2: 906 unknown + 1 parse error; re-run counters over-report (count scanned, not inserted). | One source of truth; M1 milestone. | Kurt + Claude + Codex |
| 2026-06-11 | REFACTOR BUILD START (Claude orchestrating, Codex ingest track). M3 SHIPPED: Wednesday ops run now also runs routing post-mortem + cohort health (exception-only escalation, on-time floor 93%). M4 risk-label bug VERIFIED already fixed (score_risk parity, weather fallback). M1 in build (Codex, vs DB copy). M6 automation end-state designed (`_outputs/reports/2026-06-11-M6-automation-endstate-design.md`) — goal: Kurt out of the operational loop, digest+approve only. Veho GroundPlusSuite format change documented (new parser required; GP-Zero tier; IN+TN hubs only; never ingest unverified carrier×hub). | Kurt mandate: "automate me out of this loop; take the refactor over." | Kurt + Claude |
| 2026-06-11 | Created this system-of-record. Documented data map, attribution pointer, two-DB issue, North Star routing, ice/cost rules. | Unify the pipeline; stop re-discovering the same facts. | Kurt + Claude |
| 2026-06-11 | Rule: Fixed_Route customer locks are sacred — never cost/TNT-override (anecdotal customer reason behind each). | Honor customer carrier preferences. | Kurt |
| 2026-06-11 | Hardened `weekly_carrier_report.py` — `Order::%` (fulfillment) issue types HARD-EXCLUDED from shipping buckets. Verified Gorgias sync captures all views + tags `Shipping::%` vs `Order::%` correctly (no view-988673 gap). | Stop fulfillment issues counting as shipping. | Kurt |
| 2026-06-11 | Added §3.5 REPORTS WE RUN (carrier report + Gorgias issue report + derived transit/post-mortem) and §3.6 SUB-COHORTS, HUBS & TAG RULES (A=multi-hub all-carriers/Veho; B=Dallas-only no-Veho/Express; tag grammar). | Master was missing the report catalog + sub-cohort hub capabilities (existed only in vault). | Kurt |
| 2026-06-11 | RECONCILED attribution conflict: §5 now documents TWO valid attributions — pointer (`tracking_order_link`) for invoice COVERAGE/order-joins; `cohort_key` (`cohort_attribution.py`, Veho tender-offset) for WALLET-SHARE/physical-week. Corrected my over-strong "never cohort_key." Root of "152%" = double-source invoice rows → dedup by (invoice_id, tracking). Synced master + memory + vault `Shipping Data Pipeline` + `Cohort Attribution Rules`. | Stop the docs contradicting each other on cohort attribution. | Kurt |
| 2026-06-11 | Created MASTER-HANDOFF-coldchain-refactor.md — full component inventory (every script across 3 repos + KEEP/CONSOLIDATE/RETIRE status) + refactor thesis. Ingested 2 attached invoices + 23 more (FedEx/UPS→06-02). | Fresh-agent entry point for a big-refactor epic session. | Kurt |

*(When you change the process, add a row AND update the relevant section below.)*

---

## 1. 🌟 THE GOAL (plain English)
Every box must arrive **on time, still cold, without overspending on shipping.**

To do that, for each order our system (called **Kori**) picks two things:
1. **How much ice** (gel packs) the box needs — based on the weather and how long it'll be in transit.
2. **Which carrier + hub** ships it — the cheapest one that still delivers in **2 business days or less**.

**Carrier cost order (use the cheapest that still arrives in ≤2 days):**
> **Veho (~$6) → OnTrac (~$8) → FedEx Home Delivery (~$15) / UPS Ground (~$11) → LAST RESORT: FedEx 2Day Express (~$25)**

That's the **North Star (updated + Kurt-signed 2026-07-29 — see AppyHour/CLAUDE.md)**: lowest EXPECTED TOTAL cost — cheap proven ground first (OnTrac, then FedEx Home Delivery / UPS Ground; **Veho SUSPENDED all hubs 2026-07-29**, restore = config toggle), late risk PRICED into every comparison (`cost_per_late` lever), and FedEx 2Day Express only when nothing proven-or-vouched delivers ≤2. Two floors never for sale: the 2-day promise and cold arrival.

---

## 2. 🔄 HOW DATA FLOWS (plain English)
We pull data from **6 places**, and it all lands in **one database** so Kori and our reports can use it together:

| We get… | …from | …which tells us |
|---|---|---|
| Orders + tracking + ship-week tags | **Shopify** | what shipped, when, to where, in which weekly batch |
| Shipping cost + service + hub | **Carrier invoices** (FedEx, UPS, OnTrac, Veho — by email/download) | what each box actually cost and how it went |
| Pickup + delivery dates | **ParcelPanel** | how long it really took (was it on time?) |
| Customer complaints ("arrived warm", "delayed") | **Gorgias** | which boxes failed and why |
| Weather | weather service | how hot it was on each lane (drives ice) |
| Delivery ground-truth (when needed) | **17track** | the real delivery date when ParcelPanel lags |

**The catch:** a box's cost (from the invoice) and its order are linked by **tracking number**. We use a pointer table (`tracking_order_link`) to connect tracking → order → ship-week. **Always use that pointer** to figure out which week an invoice belongs to (see §5 — using the raw invoice date gives wrong answers).

---

## 3. ✅ THE RULES EVERYONE MUST FOLLOW
1. **One database is the truth:** `%APPDATA%/AppyHour/shipping.db`. Kori reads it. Don't write shipping data anywhere else.
2. **Cohort = the `_SHIP_<Monday>` tag**, never a carrier scan date. Sub-cohort A = Fri/Sat build→Mon ship; B = Tue/Wed build→Tue ship (Dallas-only, no Veho). **`ship_week` (= this `_SHIP_<Mon>` tag) is distinct from `ship_date` (the ACTUAL departure day): A departs Mon (ship_date = ship_week's Monday), B departs TUE (ship_date = that Tuesday) — same ship_week.** ship_date drives ice/weather/vF-filename/ProductionDay; ship_week is the engine cohort key AND the allocate scope (ALL sub-cohorts of the week carry it). **Do NOT force ship_date to Monday for a Tuesday batch** (the old "always Monday" rule was wrong — it under-iced/mis-labeled Tuesday) and **do NOT allocate `available` on a single RMFG sub-tag** (under-counts sibling sub-cohorts → over-promises). See MATRIX_RULES rule 12.
3. **Two attributions, pick by question (see §5):** invoice COVERAGE + order joins → the **pointer** (`tracking_order_link`→`_SHIP` tag); WALLET-SHARE / physical carrier-week → `cohort_key` (via `cohort_attribution.py`, deduped). Don't compare counts across the two.
4. **FedEx cost must always be shown by service** — 2Day Express ($25) is very different from Home Delivery ($15). Never quote a blended FedEx number.
5. **Fixed_Route customers are sacred** — they're locked to (or away from) a carrier for an anecdotal reason (a past complaint / preference). **Never override for cost or TNT.** If honoring their lock leaves no on-time lane, it's a manual decision (Express reship / contact them) — never force them onto the carrier they avoided.
6. **Ice is physics-first** — give the least ice that keeps margin ≥ 0; history only adds more, never less. Max ice can't hold → mark CRITICAL.
7. **ParcelPanel lags 1–3 days** — "undelivered" usually means "delivered, not yet recorded." Verify with 17track before calling anything late.
   `delivery_status.status='aged_out'` is now the terminal state for stale rows that aged out without a carrier-confirmed delivery; the daily sync retires them so they do not masquerade as active `lost_in_transit` / `in_transit` packages.
8. **On-time = delivered in ≤2 days ÷ the FULL cohort** (not just delivered orders — that fakes 100%).
9. **OnTrac = LaserShip** (same carrier, two brand names on scans).
10. **Engine lane data is keyed by `carrier × hub × SERVICE`** — air and ground are different products, so air history must NEVER prove out or price a ground lane. FedEx 2Day *air* (~2-day because it flies, 32% of FedEx volume) once pooled into the FedEx Home Delivery *ground* bucket and the engine tagged a Dallas→Oregon-coast box "proven 2-day ground" (order 152472, 2026-06-17). Any new aggregation feeding a routing decision (lane proof `carrier_eff`, trial `state_eff`, delay gate `lane_late_rate`, cost `rate_card`) must filter to ground services via `lib/origin.is_ground_service()`. Receipt: `~/.knowledge/decisions/air-pollutes-ground-lane-proof-2026-06-17.md`.
11. **Touch the data, update the automation — same change.** Any change to a database (schema, ingest/write path, a read-path query/filter, or a derived cache like `zone_floor.json`) MUST pair with auditing + updating EVERY recurring job that reads/writes/derives from it: Windows Task Scheduler (`appyhour_sync_on_logon`, `AppyHour Weather Actuals`, `appyhour_daily_*`, `AppyHour Zone Floor Rebuild`, …) AND the MCP scheduled-tasks. Confirm each uses the new logic/canonical DB; give any new derived cache its own rebuild schedule; never leave a job running old logic against changed data. Checklist + receipt: `~/.claude/.../memory/feedback_db_change_update_recurring.md`.
12. **In summer, 2-day is a HARD ceiling — ICE IS NOT A LEVER PAST 2 DAYS (Kurt 2026-06-19).** No gel config we run survives 3 days in summer; it is physically impossible. So **a >2-day lane is a ROUTING problem, never an ice problem** — the only fix is a faster ground lane (≤2) or air. Never "ice for the 3-day transit"/"size more ice to cover it" (`min_ice` just returns EXCEEDED). The zone ice-floor's job is to FLAG a >2 lane → force reroute/Express (the pre-apply HARD gate + Tuesday far-zone Express), NOT to ice it. **A 3-day delivery in summer = a WARM box, never "acceptable grace"** — on-time = delivered ≤2 business days, everything else is a warm miss. A box already shipped on a 3-day lane is ALREADY warm — un-fixable; the only move is a reship on a ≤2/air lane. Receipt: 6/15 Tue cohort (air-pollution 111) = 42% warm, 106 FedEx. See rule #6 (ice physics-first) + `~/.claude/.../memory/feedback_summer_tnt2_hard_max.md`.

---

## 3.5 🗓️ REPORTS WE RUN
| Report | What it shows | Cadence | Source / doc |
|---|---|---|---|
| **Weekly Carrier Report** | carrier MIX trend + spike flags + shipping ISSUES by carrier×bucket + COST by carrier×service (FedEx HD/Ground combined; 2Day separate) | weekly (Wed) | `appyhour-shipping-data/queries/weekly_carrier_report.py --auto` |
| **Weekly Shipping Issue Report** | Gorgias CS issues (Warm/Delayed/Lost/Damaged) by carrier, reconciled vs Slack `#reship-and-order-requests` ground truth | weekly (Tue, prior Mon–Sun ticket window) | `~/.knowledge/ops/Weekly Shipping Issue Report.md` runbook |
| **Derived transit times / post-mortem** | actual TNT (pickup→delivery, **business days**) vs the 2-day promise; on-time = delivered≤2 ÷ FULL cohort; warm/delayed **vs Kori snapshot** | weekly + ad-hoc | `ShipRouting/cohort_health.py`, `routing_postmortem.py`; method = `~/.knowledge/decisions/Universal transit calculation rule.md` |
| **Anomaly scan** ("what needs attention") | 8 detectors vs each lane's own trailing baseline (not raw counts), severity-sorted, evidence + action per finding: lane-late-rate spike (3σ) · warm clusters (state×carrier) · $/box drift (>12%) · Express-share creep (vs ~11%) · **fence violations** (actual carrier vs `!NO` tags = the live B1 monitor, ANY hit CRITICAL) · matured still-out · feed freshness · address-quality. Quiet = healthy. **Not a new report — ADDITIONS:** CS-actionable flags → Slack issue report; trend flags → cost-sheet ANOMALIES tab; feed-health → notify channel. | weekly (folds into Wed post-mortem) + ad-hoc ("anomaly scan") | `~/.claude/skills/appyhour-shipping-data/queries/anomaly_scan.py` |

**Two report rules:** (1) shipping issues count `Shipping::%` only — `Order::%` (fulfillment) is a SEPARATE report, never mixed in. (2) Derived TNT uses business days (skip weekends), pickup_date→delivery_date from `delivery_status`, verified against 17track when ParcelPanel lags.

## 3.6 🏭 SUB-COHORTS, HUBS & TAG RULES
Two sub-cohorts per `_SHIP_<Mon>` week (by the `RMFG_<date>` tag), with **different hub + carrier capabilities** — the engine and CS must respect them:

| Sub-cohort | Build → ship | Volume | Hubs | Carriers | TNT note |
|---|---|---|---|---|---|
| **A** (Sat cohort) | Fri/Sat build → **Mon** pickup | ~70% | **Multi-hub: TN, TX, IN, CA** | ALL incl **Veho** | multi-hub → 2-day reliable to most zones |
| **B** (Tue cohort) | Tue/Wed build → **Tue** pickup | ~20% | **Dallas (TX) ONLY** | FedEx / OnTrac / UPS — **NO Veho** | Dallas-only → zones 5-7 can't hit 2-day on Ground → **must use FedEx 2Day Express for SLA** |

Hubs are **TN / TX / IN / CA only** (not MA). The v2 routing engine runs for **A only**; B is Dallas-only.

**Tag grammar** (`!ANY`-solo / `!NO`-stack): `!ANY - <hub>_AHB!` = positive single-hub assign (only ever Veho/OnTrac); `!NO <carrier> - <hub>_AHB!` = fence a bad lane (FedEx/UPS steered by blocking, never positively tagged); `!FedEx 2Day OneRate - Dallas_AHB!` = Express floor. Ice tags: `!ExtraGel24oz!`, `!ExtraGel48oz!` (⚠️ Shopify rejects duplicate tags — need quantity-encoded tag for >1, see M4). Full grammar: `~/.knowledge/codebase/Routing rule engine (Kori).md` + `ShipRouting/ENGINE_GUIDE.md` §2.
Detail: `~/.knowledge/ops/Shipping Cohort Hub Structure.md`.

## 4. 🗂️ DATA MAP (technical)
Full schema, row counts, and freshness: **`.claude/plans/2026-06-11-BUILD-HANDOFF-unified-coldchain-pipeline.md` §2.** Quick reference:

| Table | What it is | Key columns |
|---|---|---|
| `fulfillments` | shipped order-lines; **cohort authority** (`tags` = `_SHIP_` + `RMFG_`) | order_number, tags, tracking_number, fulfilled_at, dest_zip |
| `tracking_order_link` | 🔑 **the pointer**: tracking → order# | tracking (PK), order_number |
| `shipments` | invoice line items (cost) | tracking, carrier, service, hub, cost, ship_date, cohort_key⚠, is_internal |
| `delivery_status` | ParcelPanel pickup/delivery; stale unrecovered rows retire to terminal `status='aged_out'` | tracking_number, pickup_date, delivery_date, status |
| `feedback` | Gorgias CS issues | order_number, issue_type, date_reported, gorgias_link |
| `kori_snapshots` / `kori_snapshot_orders` | Kori's per-cohort/per-order prediction record | predicted_config, predicted_risk, margin_btu, dest_peak_temp_f |
| `shopify_orders` | order header (price, status, tags) | order_name, ship_tag, total_price |
| `weather_history` | per-zip daily temps (zip_prefix = 5-digit) | zip_prefix, date, peak_temp |
| `gel_apply_log` | when ice tags were applied + margin | ship_tag, order_number, applied_at, apply_margin_btu |
| `invoices` | invoice-email metadata | carrier, invoice_week, total_balance |

---

## 5. 🔑 THE ATTRIBUTION CHAIN (technical — how cost → cohort)
**There are TWO valid attributions. Pick by the QUESTION you're asking — they legitimately diverge (rolled-forward orders + carrier tender offsets).**

**(a) ORDER attribution (pointer) — use for invoice COVERAGE + order-level reconciliation:**
```
shipments.tracking → tracking_order_link → order_number
  → fulfillments.tags → _SHIP_<Mon> (cohort) + RMFG_<date> (sub-cohort A/B)
```
Answers "of the orders tagged `_SHIP_X`, how many have an invoice / what did they cost." This is the one for coverage %, CS/order joins, and the per-cohort cost we report.

**(b) PHYSICAL-WEEK attribution (`cohort_key`) — use for WALLET-SHARE / carrier spend by the week packages physically shipped:**
`shipments.cohort_key` via `GelPackCalculator/cohort_attribution.py::cohort_for()` — carrier ship_date − day-of-week, with **Veho tender-date offset** (Veho's date = Sat tender, picks up next Mon). Authoritative for "what physically shipped this carrier-week" (carrier negotiations). Detail: vault [[Cohort Attribution Rules]].

⚠️ **Do NOT compare counts ACROSS (a) and (b)** — they answer different questions; ~rolled-forward orders make them differ by design. My earlier "cohort_key 56%/152% vs pointer 100%" was an apples-to-oranges compare (order-tag coverage vs physical-week) **compounded by a real double-source bug**: the same invoice exists in both the RMFG breakdown XLSX and the FedEx/UPS account CSV → cohort_key double-counted (the 152%). **Fix = dedup invoice rows by (invoice_id, tracking) before any cohort_key rollup.** Once deduped, cohort_key is correct for wallet-share; the pointer stays the one for coverage.

---

## 6. 🧠 THE DECISION COMPONENTS (technical)
- 📕 **CANONICAL ROUTING + ICE RULEBOOK → `ShipRouting/ROUTING_RULES.md`** — every lane / proof / Express /
  reship / Indy-cap / ice / season / feasible-hub rule in ONE place, single source of truth. §3 above is the
  org-wide *subset*; ROUTING_RULES.md is the full detail. **Change a routing rule THERE first.**
- **ShipRouting `lib/engine.py compute_routing()`** — the cost-aware routing brain (North Star + TNT≤2 survivor rule). It is **THE config now (not "shadow")** — `build.py`, `apply_shadow_ice.py`, AND Kori (`is_pipeline_v2=True`, G14 2026-06-12) all decide routing + ice from it. **Routing is fully unified; ICE has 3 entry points that can differ** (audit 2026-06-12): engine shadow uses DB-avg temp; Kori re-sizes with LIVE forecast temp (upgrade-only); `apply_shadow_ice` adds a summer-worst-case downgrade guard. Convergence tracked as G18. Guide: `ShipRouting/ENGINE_GUIDE.md`.
- **MILP Indy-capacity draft (`ShipRouting/milp/draft_sheet.py`)** — runs the cohort through carrier-TNT + a global HiGHS solve that picks the globally cheapest keep/spill set under the 6-pallet Indy cap, and emits a DRAFT routing xlsx. **This MAKES the Indy decision, but is HUMAN-GATED — surfaced as a draft for review before apply, NOT autonomous.** Until the draft workflow is adopted, live `compute_routing` still ships via the greedy `_indy_pallet_gate`. A/B trade: ~$627/wk hard CASH saved vs +$234/wk warm-risk → ~$393/wk NET. Guide: `ShipRouting/milp/`.
- **Kori** (`GelPackCalculator/kori/gel_pack_webview.py`) — assigns ice + routing, records Lock&Ship. Launch: `run_webview.bat`. Reads `%APPDATA%` via `appyhour_lib/paths.py::db_path()`.
- **Invoice importer (live):** `GelPackCalculator/sync_all_carriers.py` → APPDATA (now FedEx+OnTrac+Veho). ⚠️ NOT `ShippingReports/ingest_all.py` (builds a dead duplicate DB).

---

## 7. 🚧 WHAT WE'RE STILL BUILDING
🧭 **Fresh epic / big-refactor entry point: `.claude/plans/2026-06-11-MASTER-HANDOFF-coldchain-refactor.md`** — full component inventory (every script + KEEP/CONSOLIDATE/RETIRE status), refactor thesis (~11 overlapping importers → 1, two DBs → 1), and current state. Start a new `/forge epic` session there.
Roadmap: **`.claude/plans/2026-06-11-EPIC-unified-coldchain-pipeline.md`** (M1 one-DB + pointer backfill · M2 full ingest + weather actuals · M3 weekly post-mortem · M4 ice enforcement: box-upgrade + reship gel + Shopify dup-tag fix · M5 routing go-live).
Open items: retire `output/shipments.db`; backfill `cohort_key`; weather actuals not synced; ice gate can't add more ice yet (no box-upgrade + Shopify rejects duplicate gel tags); ShipRouting SHADOW→APPLY (resolve Bree/Pam Fixed_Routes); adopt the MILP `draft_sheet.py` Indy-decision workflow (today it's drafted + human-reviewed, but live ships the greedy `_indy_pallet_gate`).
⚠️ **Offsite backup is BROKEN (local-only).** `scripts/backup_offsite.py` snapshots `shipping.db` to `%APPDATA%/AppyHour/backups/`, but the Drive upload step (`gws drive +upload`) fails — **`gws` is not installed / not on PATH** — so the "weekly offsite" has been **LOCAL-ONLY**. Fix = reinstall/auth the `gws` CLI or swap the uploader (rclone / Drive API). (Code itself IS offsite via the GitHub repos.)
📝 **Deployment note: canon = live.** Branch **`today-experiments-2026-07-03`** (GitHub `kurt-lgtm/ShipRouting`) is PRODUCTION — Kurt 2026-07-18 (wk0720 shipped from it; **supersedes `restore/cohort-scripts-2026-06`**). Prod imports the working tree at `C:\Users\Work\Claude Projects\ShipRouting`; editing those files IS editing prod.

---

## 8. 📁 WHERE THINGS LIVE
- DB: `%APPDATA%/AppyHour/shipping.db` · path helper: `appyhour_lib/paths.py`
- Importer: `GelPackCalculator/sync_all_carriers.py` · parsers: `ShippingReports/parsers/`
- Kori: `GelPackCalculator/kori/` · Routing: `ShipRouting/` (+ `ENGINE_GUIDE.md`)
- Weekly report: `~/.claude/skills/appyhour-shipping-data/queries/weekly_carrier_report.py --auto`
- Deep build handoff: `.claude/plans/2026-06-11-BUILD-HANDOFF-unified-coldchain-pipeline.md`

---
*Change the process? Add a CHANGE LOG row + update the section. That's the deal.*
